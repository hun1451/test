<?xml version="1.0" encoding="utf-8"?>

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kr.ac.chosun.test">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="test"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="kr.ac.chosun.test.initActivity"
            android:theme="@android:style/Theme.NoTitleBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".congestion" />
        <activity android:name=".parking" />
        <activity android:name=".delay" />
        <activity android:name=".takeoffinfo" />
        <activity android:name=".landing" />
        <activity android:name=".convenience" />


        <activity
            android:name=".MainActivity"
            android:label="test" >
        </activity>
    </application>

</manifest>